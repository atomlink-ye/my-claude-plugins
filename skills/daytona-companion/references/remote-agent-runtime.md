# Deprecated remote-agent runtime reference

This reference is a tombstone. The former remote Paseo/OpenCode bootstrap workflow is no longer supported by the Daytona companion skill. Do not bootstrap remote agents or place provider secrets in a sandbox.

Use `sandbox-ctl` for direct Daytona lifecycle, command execution, preview, and artifact transfer. Keep orchestration agents local and pass only the minimum non-secret command inputs to a sandbox.
