# Provider-owned subagent observation (redacted)

Live `PaseoAdapter.providerSubagents` against parent `ecce5f94…` returned `source: provider_subagents` and three safely projected completed Codex descriptors: Source map, Counterexamples, and CLI dogfood reviewer. Each projection contained only id, provider, title, status, createdAt, and updatedAt. No description, tool call id, cwd, subtitle, timeline, prompt, or native handle was emitted.

Provider RPC failure remains `source: unavailable` and does not block an ARCP worker; native members are not reconstructed.
